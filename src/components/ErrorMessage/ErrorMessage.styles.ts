import styled from "styled-components";

export const ErrorWrapper = styled.div`
  position: fixed;
  top: 1rem;
  left: 50%;
  transform: translateX(-50%);
  width: fit-content;
  max-width: 45rem;
`;
